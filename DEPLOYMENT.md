# 🚀 Деплой на Vercel

Инструкция по безопасному разворачиванию PromtForge на Vercel.

---

## 🔐 ВАЖНО: Безопасность ключей

### Проблема
Файл `.env` с Supabase ключами **никогда не должен** быть в git репо. Это критическая уязвимость!

### Решение
1. ✅ `.env` уже добавлен в `.gitignore`
2. ✅ Используй `.env.example` как шаблон
3. ✅ Переменные передаются через Vercel Dashboard (не через git)

---

## 📋 Пошаговая инструкция

### 1️⃣ Локально: Подготовка

**Если вы уже добавили `.env` в репо (одноразово):**

```powershell
# Удалить .env из git истории (защита ключей)
git rm --cached .env
git add .gitignore
git commit -m "🔐 Remove .env with secrets from tracking"
git push origin main
```

**Затем создайте локальный `.env` (не будет отслеживаться):**
```bash
cp .env.example .env
# Отредактируйте .env с вашими реальными ключами для разработки
```

---

### 2️⃣ GitHub: Убедитесь, что готово

```bash
# Проверить статус репо
git status

# Должно быть:
# ✅ .env не показывается (в .gitignore)
# ✅ .env.example есть в репо
# ✅ vercel.json есть в репо
```

---

### 3️⃣ Vercel Dashboard: Развернуть

#### Вариант A: Первый раз (рекомендуется)

1. Перейти на [https://vercel.com](https://vercel.com)
2. Нажать **"Add New..."** → **"Project"**
3. Выбрать репо **`intuit-prompt`**
4. Vercel автоматически определит:
   - ✅ Framework: **Vite**
   - ✅ Build Command: **`npm run build`**
   - ✅ Output Directory: **`dist`**

5. **ПЕРЕД деплоем**: Нажать **"Environment Variables"**
6. Добавить 3 переменные:

| Название | Значение | Область |
|----------|----------|---------|
| `VITE_SUPABASE_PROJECT_ID` | `mocjwuzkzrcaelhkxhdg` | Production, Preview, Development |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` | Production, Preview, Development |
| `VITE_SUPABASE_URL` | `https://mocjwuzkzrcaelhkxhdg.supabase.co` | Production, Preview, Development |

7. Нажать **"Deploy"**

#### Вариант B: Если уже развёрнут

1. Перейти на [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. Выбрать проект **`intuit-prompt`**
3. Перейти **Settings** → **Environment Variables**
4. Добавить/обновить 3 переменные (см. таблицу выше)
5. Перейти **Deployments** → нажать **Redeploy** на последнем деплою

---

### 4️⃣ Локально: Тестирование build'а

Перед деплоем на Vercel проверьте build локально:

```bash
# Собрать приложение
npm run build

# Проверить результат
npm run preview
# Откроется на http://localhost:4173
```

**Проверить:**
- [ ] Нет ошибок в консоли
- [ ] UI загружается корректно
- [ ] localStorage работает
- [ ] Supabase интеграция инициализируется без ошибок

---


## 📋 Пошаговая инструкция (Vercel интеграция)

### 1) Подготовка локально

1. Убедитесь, что `.env` не отслеживается Git и у вас есть `.env.example` как шаблон.

```powershell
git rm --cached .env || echo "no .env tracked"
git add .gitignore
git commit -m "chore: ignore local .env"
git push origin main
```

2. Проверьте, что `vercel.json` присутствует (в корне) и `package.json` содержит скрипты `build` и `preview`.

### 2) Подключение репозитория к Vercel

1. На https://vercel.com нажмите **Add New → Project**.
2. Выберите GitHub аккаунт и найдите репозиторий **`PromtForge`** (уже должен быть доступен).
3. Vercel автоматически определит:
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`

4. В разделе **Environment Variables** добавьте необходимые ключи (Production/Preview/Development):

```text
VITE_SUPABASE_PROJECT_ID    # (если используете Supabase — иначе пропустите)
VITE_SUPABASE_PUBLISHABLE_KEY
VITE_SUPABASE_URL
```

> Примечание: если вы не используете Supabase, не добавляйте эти переменные — проект работает локально с `localStorage`.

5. Нажмите **Deploy**. После первого деплоя последующие commits автоматически запустят деплой.

### 3) Локальное тестирование build'а

```bash
npm run build
npm run preview
# Preview по умолчанию доступен на http://localhost:4173
```

### 4) Проверки после деплоя

- Убедитесь, что приложение загружается и нет ошибок в консоли
- Если вы используете Supabase — проверьте, что интеграция инициализируется корректно
- Проверьте, что `promtforge-state-v1` присутствует в `localStorage` для корректной работы

---

## 🔧 Автоматическая связь с Vercel (альтернатива — Vercel CLI)

Если вы хотите связать проект из терминала и у вас есть Vercel аккаунт, можно использовать `vercel` CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# В папке проекта
vercel login
vercel link   # выберите/создайте проект в вашей учетной записи
vercel --prod # выполнить продакшн деплой
```

Vercel CLI потребует авторизации и выбора проекта; это нельзя автоматизировать без доступа к вашей учётной записи.

---

Если хотите — могу помочь добавить GitHub -> Vercel интеграцию вручную через Vercel Dashboard (ваш вход в систему необходим), или подготовить GitHub Actions/CI для альтернативного автоматического деплоя.
---
