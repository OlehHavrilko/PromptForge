# 🚀 Деплой на Vercel

Инструкция по безопасному разворачиванию Prompt Manager на Vercel.

---

## 🔐 ВАЖНО: Безопасность ключей

### Проблема
Файл `.env` с Supabase ключами **никогда не должен** быть в git репо. Это критическая уязвимость!

### Решение
1. ✅ `.env` уже добавлен в `.gitignore`
2. ✅ Используй `.env.example` как шаблон
3. ✅ Переменные передаются через Vercel Dashboard (не через git)

---

## 📋 Пошаговая инструкция

### 1️⃣ Локально: Подготовка

**Если вы уже добавили `.env` в репо (одноразово):**

```powershell
# Удалить .env из git истории (защита ключей)
git rm --cached .env
git add .gitignore
git commit -m "🔐 Remove .env with secrets from tracking"
git push origin main
```

**Затем создайте локальный `.env` (не будет отслеживаться):**
```bash
cp .env.example .env
# Отредактируйте .env с вашими реальными ключами для разработки
```

---

### 2️⃣ GitHub: Убедитесь, что готово

```bash
# Проверить статус репо
git status

# Должно быть:
# ✅ .env не показывается (в .gitignore)
# ✅ .env.example есть в репо
# ✅ vercel.json есть в репо
```

---

### 3️⃣ Vercel Dashboard: Развернуть

#### Вариант A: Первый раз (рекомендуется)

1. Перейти на [https://vercel.com](https://vercel.com)
2. Нажать **"Add New..."** → **"Project"**
3. Выбрать репо **`intuit-prompt`**
4. Vercel автоматически определит:
   - ✅ Framework: **Vite**
   - ✅ Build Command: **`npm run build`**
   - ✅ Output Directory: **`dist`**

5. **ПЕРЕД деплоем**: Нажать **"Environment Variables"**
6. Добавить 3 переменные:

| Название | Значение | Область |
|----------|----------|---------|
| `VITE_SUPABASE_PROJECT_ID` | `mocjwuzkzrcaelhkxhdg` | Production, Preview, Development |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` | Production, Preview, Development |
| `VITE_SUPABASE_URL` | `https://mocjwuzkzrcaelhkxhdg.supabase.co` | Production, Preview, Development |

7. Нажать **"Deploy"**

#### Вариант B: Если уже развёрнут

1. Перейти на [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. Выбрать проект **`intuit-prompt`**
3. Перейти **Settings** → **Environment Variables**
4. Добавить/обновить 3 переменные (см. таблицу выше)
5. Перейти **Deployments** → нажать **Redeploy** на последнем деплою

---

### 4️⃣ Локально: Тестирование build'а

Перед деплоем на Vercel проверьте build локально:

```bash
# Собрать приложение
npm run build

# Проверить результат
npm run preview
# Откроется на http://localhost:4173
```

**Проверить:**
- [ ] Нет ошибок в консоли
- [ ] UI загружается корректно
- [ ] localStorage работает
- [ ] Supabase интеграция инициализируется без ошибок

---

## 🔧 Структура файлов для деплоя

```
project/
├── .env                    # 🔴 ЛОКАЛЬНО (не в git)
├── .env.example            # ✅ В git (шаблон)
├── .gitignore              # ✅ Исключает .env
├── vercel.json             # ✅ Конфигурация Vercel
├── vite.config.ts          # ✅ Вите конфиг
├── package.json            # ✅ Зависимости
└── src/                    # ✅ Исходный код
```

---

## 📊 Environment Variables в Vercel

### Зачем нужны?

Vercel **не может** просто так прочитать `.env` файл (его нет в репо). Поэтому переменные передаются через Dashboard:

```
GitHub Repo → Vercel CI/CD → Build процесс → Environment Variables → .env (на сервере)
```

### Где взять значения?

1. **VITE_SUPABASE_PROJECT_ID** — из `.env` локально (или Supabase Dashboard)
2. **VITE_SUPABASE_PUBLISHABLE_KEY** — из `.env` локально (или Supabase Dashboard)
3. **VITE_SUPABASE_URL** — из `.env` локально (или Supabase Dashboard)

**Supabase Dashboard:**
- Перейти на [https://app.supabase.com](https://app.supabase.com)
- Выбрать проект
- Перейти **Settings** → **API**
- Скопировать нужные значения

---

## ✅ Чек-лист перед деплоем

- [ ] `.env` удалён из git (`git rm --cached .env`)
- [ ] `.env.example` есть в репо
- [ ] `.gitignore` содержит `.env`
- [ ] `vercel.json` есть в репо
- [ ] `npm run build` работает локально без ошибок
- [ ] `npm run preview` показывает приложение корректно
- [ ] Environment Variables добавлены в Vercel Dashboard (3 переменные)
- [ ] GitHub репо в актуальном состоянии (`git push`)

---

## 🎯 Типовые ошибки и решения

### ❌ "Cannot find module supabase"
**Причина:** Переменные окружения не передались в build.
**Решение:** Проверьте, что переменные добавлены в Vercel Dashboard и выберите **Redeploy**.

### ❌ "localStorage is not defined"
**Причина:** Код выполняется на сервере вместо клиента.
**Решение:** Убедитесь, что `localStorage` используется только в компонентах (`useEffect` или `useLayoutEffect`).

### ❌ "Build failed"
**Решение:**
1. Проверьте `npm run build` локально
2. Посмотрите Vercel logs (Deployments → Failed)
3. Проверьте TypeScript ошибки: `npm run lint`

### ❌ "Blank page / 404"
**Причина:** Неправильный outputDirectory.
**Решение:** Убедитесь, что `vercel.json` содержит `"outputDirectory": "dist"`.

---

## 🚀 После деплоя

1. **Проверить URL** — откроется ваше приложение
2. **Домен** — по умолчанию `https://intuit-prompt.vercel.app`
3. **Custom домен** (опционально) — Settings → Domains в Vercel

---

## 📚 Полезные ссылки

- [Vercel Docs](https://vercel.com/docs)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html#vercel)
- [Supabase Dashboard](https://app.supabase.com)
- [React Router SPA Fallback](https://vercel.com/support/articles/how-do-i-use-the-public-folder-for-routing-in-my-spa)

---

**Успешного деплоя! 🎉**
