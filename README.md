# PromtForge

**Веб‑приложение для управления и конструирования AI‑промптов** — персональный инструмент для создания, организации и переиспользования промпт‑шаблонов с поддержкой переменных, теггирования и локального хранения.

Построено с ❤️ на **React** + **TypeScript** + **Vite** + **Tailwind CSS** + **shadcn/ui**.

---

## 🎯 Цели MVP

1. **Быстрая разработка** — готовый инструмент для личного использования за 1–2 недели.
2. **Локальность** — полная независимость от серверов (localStorage как хранилище).
3. **Приватность** — все данные остаются в браузере пользователя.
4. **Расширяемость** — архитектура готова для подключения синхронизации и бэкенда.

---

## ✨ Ключевые фичи (MVP)

### 📚 Каталог промптов
- **Список всех промптов** в современном тёмном UI
- **Быстрый просмотр** — превью текста и метаданных
- **Сортировка и группировка** — по дате создания, названию, популярности

### 🔄 CRUD операции
- ✅ **Create** — новый промпт через форму с валидацией
- ✅ **Read** — детальный просмотр с полной информацией
- ✅ **Update** — редактирование всех полей промпта
- ✅ **Delete** — удаление с подтверждением
- ✅ **Duplicate** — быстрое копирование как основу для нового промпта

### 🏷️ Система тегов и коллекций
- **Теги** — неограниченное количество для каждого промпта
- **Коллекции** — группы промптов по смыслу (e.g., "Writing", "Analysis", "Coding")
- **Цветовое кодирование** — для быстрой визуальной идентификации
- **Привязка промптов** — каждый промпт может принадлежать одной коллекции

### ⭐ Избранные
- **Toggle favorite** — сохранение часто используемых промптов
- **Фильтр по избранным** — быстрый доступ к top-используемым

### 🔍 Поиск и фильтры
- **Full-text поиск** по:
  - Названию (`title`)
  - Описанию (`description`)
  - Тексту промпта (`body`)
  - Тегам (`tags`)
  - Коллекции
- **Комбинированные фильтры** — одновременно по тегам, коллекции, статусу
- **Реалтайм результаты** — при вводе без задержек

### 🔢 Переменные в промптах
- **Синтаксис `{{variable}}`** — для динамических значений
- **Метаданные переменной**:
  - `name` — идентификатор переменной
  - `description` — подсказка для пользователя
  - `defaultValue` — значение по умолчанию (опционально)
- **Подстановка** — при использовании промпта
- **Пример**:
  ```
  Ты {{role}}. Проанализируй текст про {{topic}} в контексте {{context}}.
  ```

### 📤 Импорт / Экспорт JSON
- **Экспорт** — сохранение всех промптов и коллекций в `.json` файл
- **Импорт** — загрузка из файла с мержингом или перезаписью
- **Формат** — структурированный JSON с версионированием для будущей совместимости

### 💾 Локальное хранение
- **Zustand** — состояние приложения (легко и предсказуемо)
- **localStorage** — персистентность между сеансами
- **Автосохранение** — все действия сохраняются сразу
- **Без синхронизации** — данные не синкуются между устройствами (MVP ограничение)

---

## 📊 Структура данных

### Prompt
```typescript
{
  id: string;                      // UUID
  title: string;                   // "Анализ тональности"
  description?: string;            // "Эмоциональный анализ текста"
  body: string;                    // Основной текст промпта с {{переменными}}
  tags: string[];                  // ["nlp", "sentiment", "analysis"]
  collectionId?: string;           // Привязка к коллекции
  model?: string;                  // "gpt-4" | "claude-3" | др.
  language?: string;               // "ru" | "en"
  variables?: PromptVariable[];    // [{name, description, defaultValue}]
  favorite: boolean;               // Добавлен в избранное?
  createdAt: string;               // ISO 8601
  updatedAt: string;               // ISO 8601
}
```

### Collection
```typescript
{
  id: string;                      // UUID
  name: string;                    // "Анализ текста"
  color?: string;                  // "#ff6b6b" для UI
}
```

### PromptFilters
```typescript
{
  search: string;                  // Текст поиска
  tags: string[];                  // Активные теги
  collectionId?: string;           // Фильтр по коллекции
  favoritesOnly: boolean;          // Только избранные?
}
```

---

## 🚀 Запуск локально

### Требования
- **Node.js** ≥ 18.x
- **npm** или **bun** (проект использует `bun.lockb`)

### Инструкция
```bash
# Клонирование репозитория
git clone https://github.com/OlehHavrilko/intuit-prompt.git
cd intuit-prompt

# Установка зависимостей
npm install
# или
bun install

# Разработка (hot reload)
npm run dev
# Доступно на http://localhost:8080

# Build для продакшна
npm run build

# Preview build'а локально
npm run preview
```

**Порт по умолчанию:** `8080` (настраивается в `vite.config.ts`)

---

## 🏗️ Архитектура

### Стек технологий
| Слой | Технология |
|------|-----------|
| **Frontend** | React 18 + TypeScript |
| **Сборка** | Vite (⚡ супербыстрая) |
| **Стилизация** | Tailwind CSS + PostCSS |
| **UI компоненты** | shadcn/ui (Radix UI + Tailwind) |
| **Состояние** | Zustand (минималистичное) |
| **Маршрутизация** | React Router v6 |
| **HTTP клиент** | fetch API (встроенный) |
| **Валидация форм** | @hookform/resolvers + Zod |

### Структура папок
```
src/
├── components/          # React компоненты
│   ├── ui/             # shadcn/ui компоненты (не трогаем)
│   ├── GeneratedPrompt.tsx
│   ├── PromptInput.tsx
│   └── ...
├── hooks/              # Custom React хуки
├── lib/
│   ├── promptGenerator.ts    # Бизнес-логика
│   ├── store/
│   │   └── promptsStore.ts   # Zustand стор
│   └── types/
│       └── promptTypes.ts    # TypeScript типы
├── pages/              # Страницы приложения
└── integrations/       # Интеграции (опциональные)
```

---

## ⚙️ Zustand Store (promptsStore.ts)

```typescript
// Состояние
prompts: Prompt[]                     // Все промпты
collections: Collection[]            // Все коллекции
selectedPromptId?: string            // Текущий выбранный
filters: PromptFilters              // Активные фильтры

// Действия
initFromStorage()                    // Загрузить из localStorage
addPrompt(data)                      // Создать новый
updatePrompt(id, updates)           // Обновить
deletePrompt(id)                     // Удалить
duplicatePrompt(id)                 // Скопировать
addCollection(data)                  // Создать коллекцию
updateCollection(id, updates)       // Обновить коллекцию
deleteCollection(id)                 // Удалить коллекцию
toggleFavorite(id)                   // Переключить звезду
setFilters(partial)                  // Применить фильтры
selectPrompt(id)                     // Выбрать промпт
```

**Преимущества Zustand:**
- ✅ Минимум boilerplate (vs Redux)
- ✅ Встроенная персистентность
- ✅ TypeScript-friendly
- ✅ Производительность (без лишних ре-рендеров)

---

## 📝 localStorage схема

```json
{
  "promtforge-state": {
    "prompts": [...],
    "collections": [...],
    "filters": {...}
  }
}
```

**Размер:** ~100KB на 50–100 промптов (доступно ~5–10MB в браузере).

---

## ⛔ Ограничения MVP

| Ограничение | Почему | Решение (v2+) |
|------------|--------|--------------|
| **Только localStorage** | Простота, нет бэкенда | Backend + база данных |
| **Нет синхронизации** | Данные не синкуются между устройствами | Синхронизация через API |
| **Нет версионирования** | История не сохраняется | Git-like undo/redo stack |
| **Одна коллекция на промпт** | Упрощение модели | Multi-tag collections |
| **Нет облачного backup** | Локальное хранилище | Auto-backup в облако |
| **Нет совместной работы** | Личный инструмент | Sharing & collaboration |

---

## 🗓️ Roadmap (v1.1–v2.0)

### v1.1 (Ближайшие 2 недели)
- [ ] **Локальное резервирование** — сохранение json на диск
- [ ] **Keyboard shortcuts** — ускорение workflow
- [ ] **Темизация** — светлый/тёмный режим toggle
- [ ] **Улучшенный поиск** — fuzzy search + regex support

### v1.2 (Месяц)
- [ ] **История изменений** — undo/redo для всех действий
- [ ] **Шаблоны** — готовые templates для распространённых задач
- [ ] **Статистика** — какие промпты используются чаще
- [ ] **Категоризация** — smart categorization по AI (если позволит время)

### v2.0 (Опциональная расширяемость)
- [ ] **Backend интеграция** — собственная база данных
- [ ] **Синхронизация** — синхронизация между устройствами
- [ ] **Версионирование** — история версий промптов
- [ ] **Экспорт форматов**:
  - Markdown (.md)
  - ChatGPT-ready JSON
  - Claude XML format
  - LangChain templates
- [ ] **Team collaboration** — общие промпты, комментарии, рейтинги
- [ ] **Public library** — делиться промптами с сообществом

---

## 📦 Экспорт JSON (пример)

```json
{
  "version": "1.0",
  "exportedAt": "2024-12-04T10:30:00Z",
  "collections": [
    {
      "id": "coll-1",
      "name": "Анализ текста",
      "color": "#3b82f6"
    }
  ],
  "prompts": [
    {
      "id": "pmt-1",
      "title": "Анализ тональности",
      "description": "Определить эмоциональный окрас",
      "body": "Проанализируй тональность текста: {{text}}",
      "tags": ["nlp", "sentiment"],
      "collectionId": "coll-1",
      "model": "gpt-4",
      "language": "ru",
      "variables": [
        {
          "name": "text",
          "description": "Текст для анализа",
          "defaultValue": ""
        }
      ],
      "favorite": true,
      "createdAt": "2024-12-01T08:00:00Z",
      "updatedAt": "2024-12-04T10:30:00Z"
    }
  ]
}
```

---

## 🔧 Разработка

### Главные файлы
- **`src/lib/store/promptsStore.ts`** — сердце приложения
- **`src/lib/types/promptTypes.ts`** — контракт данных
- **`src/components/PromptInput.tsx`** — форма создания
- **`src/components/GeneratedPrompt.tsx`** — карточка промпта

### Запуск в dev режиме
```bash
npm run dev
# Hot reload включен, изменения видны мгновенно
```

### Сборка для продакшна
```bash
npm run build
# Результат в dist/
npm run preview  # Тестирование build'а
```

### Линтинг и проверка кода
```bash
npm run lint      # Проверить ошибки
```

---

## 📄 Лицензия

MIT — свободно используй в личных и коммерческих проектах.

---

## 🚀 Деплой

Для подробной инструкции по деплою на Vercel смотри `DEPLOYMENT.md`.

---

**Хорошего кодирования! 🚀**

---

## 🏷️ Правила именования проектов

Проектные имена должны быть короткими, понятными и валидироваться по следующим правилам:

- Символы: только строчные латинские буквы (`a-z`), цифры (`0-9`) и символы `.` `_` `-`.
- Длина: от 1 до 100 символов.
- Запрещено содержать последовательность `---`.

Регулярное выражение для валидации:

```regexp
^(?!.*---)[a-z0-9._-]{1,100}$
```

Пример использования (реализация в `src/lib/utils/validateProjectName.ts`):

```ts
import { isValidProjectName } from './src/lib/utils/validateProjectName';

isValidProjectName('my-project'); // true
isValidProjectName('bad---name'); // false
```

Для быстрого локального теста запустите:

```bash
npm run validate:demo "my-project"
npm run test:validate
```

